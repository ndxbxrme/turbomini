# Web Components

## What it demonstrates
- Copy-mode web components (`tm-button`, `tm-input`, `tm-dialog`).
- `tm-*` event handling inside `postLoad()`.
- Component props and slots inside TurboMini templates.

## Run it
```bash
npm run dev
```
Open `http://localhost:4173/web-components/` in your browser.

## Test it
```bash
npm test
```

## Related guide
- `/guides/web-components`
